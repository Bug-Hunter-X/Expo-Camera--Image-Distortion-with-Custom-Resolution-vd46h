This bug occurs when using the Expo Camera API with a custom resolution.  The preview displays correctly, but the captured photo is distorted or of an unexpected size.  This is often due to incorrect aspect ratio handling within the app's logic.